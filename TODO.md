# TODO - Snapshot tests for key components

- [ ] Ensure Jest is configured with the React Native snapshot serializer.
- [ ] Add snapshot test for `Button`.
- [ ] Add snapshot test for `Card`.
- [ ] Add snapshot test for `SubscriptionCard`.
- [ ] Run Jest to generate snapshots in version control.
- [ ] Create git branch `blackboxai/<name>`, commit, and push to GitHub.

