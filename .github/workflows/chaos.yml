name: Chaos Engineering
on:
  push:
    branches: [main, dev, develop, 'feature/*']
  pull_request:
    branches: [main, dev, develop, 'feature/*']
jobs:
  chaos:
    uses: ./.github/workflows/chaos-core.yml
    secrets: inherit
