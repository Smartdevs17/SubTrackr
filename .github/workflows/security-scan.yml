name: Security Scan
on:
  push:
    branches: [main, dev, develop]
  pull_request:
    branches: [main, dev, develop]
  schedule:
    - cron: '0 0 * * 1'
jobs:
  security:
    uses: ./.github/workflows/security-core.yml
    secrets: inherit
