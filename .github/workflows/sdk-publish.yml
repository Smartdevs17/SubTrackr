name: SDK Packages
on:
  push:
    tags: ['sdk-v*']
  pull_request:
    paths: ['sdks/**', 'docs/openapi.yaml', 'scripts/generate-sdks.js', '.github/workflows/sdk-publish.yml']
jobs:
  sdk:
    uses: ./.github/workflows/sdk-core.yml
    secrets: inherit
