name: Contract Invariant Tests
on:
  push:
    branches: [main, dev, develop, 'feature/*']
    paths: ['contracts/**']
  pull_request:
    branches: [main, dev, develop, 'feature/*']
    paths: ['contracts/**']
env:
  RUST_VERSION: '1.88'
jobs:
  contracts:
    uses: ./.github/workflows/contracts-core.yml
    secrets: inherit
