/**
 * @deprecated Use `useStore` from `./combinedStore` instead.
 * All stores are now combined into a single store using the slices pattern.
 */
export { useStore as useInvoiceStore } from './combinedStore';
