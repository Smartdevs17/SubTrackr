[package]
name = "subtrackr-dispute"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true }
subtrackr-types = { path = "../types" }

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1